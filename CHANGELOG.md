# 更新记录 · Changelog

线上地址：<https://rmysjmzg987.github.io/rmysjmzg-personal-storage/>
版本号与 `package.json` 的 `version` 同步；v0.4 之前的历史按开发里程碑回溯整理。

---

## v0.4.0 · 2026-09-16

**气象云图模式** — Weather mode: a cloud shell, live city weather and the operational weather fleet.

### 新增

- **气象云图模式**：顶栏一键在「卫星轨道 / 气象云图」之间切换，快捷键 `M`；也支持 `?mode=weather`（直达气象模式）与 `?mode=weather&debug=1`（额外显示云图日期与状态）。
- **云壳**：地球外挂一层半径 ×1.012 的球壳，贴图取 NASA 每日全球真彩卫星影像；片元着色器按「亮度高 + 饱和度低 + 黄调弱」只保留云，海洋、沙漠、植被全部判为透明，于是只剩云悬在地球上。云壳挂在地球组内随自转走；影像只有日面数据，因此夜面不显示云（与地球夜灯面一致）。
- **城市实时天气**：102 座城市按 Open-Meteo 批量取当前天气，城市名右侧显示天气图标（晴天 / 多云按当地昼夜换成太阳或月亮），悬停出气泡：天气状况 · 气温 · 风速 · 降水。
- **专业气象卫星叠加**：右侧「气象卫星」开关显示 10 颗主力在轨气象星——GOES-16、GOES-18、向日葵 8 号、向日葵 9 号、风云四号 B、Meteosat-12、风云三号 D、风云三号 G、NOAA-20、NOAA-21。这份名单独立于你在轨道模式里对卫星的增删，切换模式不会改写你的卫星清单。
- **数据刷新与说明**：进入气象模式自动拉取一次，超过 30 分钟自动重拉，右侧「刷新数据」可随时手动重拉；「数据说明」如实标注云图日期、天气更新时间与数据来源。
- **时间语义**：云图与天气都是「此刻」的数据，进气象模式即把模拟时钟锁到本机当前时间并禁用倍速，退出时还原进入前的播放状态与倍率。
- **模式互斥**：气象模式下隐藏搜索栏、添加卫星入口与轨道线等卫星相关内容，退出后原样恢复。
- 新增模块 `src/weather/`（云图抓取与裁切、城市天气、天气图标）、`src/viz/cloudShell.ts`（云壳与云提取着色器）、`src/orbit/meteo.ts`（气象卫星名单）；新增 `tests/weather.test.ts`。

### 修复

- 修复气象模式叠加气象卫星后卫星停在切模式那一刻不动的问题（原先气象模式整段跳过卫星场景更新）。
- 修复切换模式时顶栏宽度随状态文本长短跳动的问题。

### 文档

- 设计文档新增 §3.7 气象云图模式需求（FR-71–FR-78）、§7 新模块、§9.1 新数据源、§14.2 v0.4 决策记录（D23–D33）、§15 风险与 §16 参考。
- 中英文 README 补齐气象模式章节、操作指南、数据来源与已知限制。
- 新增本文件。

---

## v0.3.0 · 2026-09-16

**首个上线版本** — First public release: camera feel, search dock, orbit manager and a higher-resolution Earth.

### 新增

- **底部搜索栏**：常驻页面底部（`/` 聚焦），按本地化名称前缀 > 名称包含 > 中英原名（别名）> NORAD 编号 > 轨道分组名实时匹配，最多 40 条；点结果或回车即把该卫星加入场景并锁定跟随。
- **轨道管理页**：添加卫星弹窗的第四个 tab，列出当前画面上全部卫星并可逐颗移除（预制卫星同样可移除），另有「恢复默认」与「全部移除」。
- **视角切换**：`V` 键或详情卡「视角」按钮，在俯视（相机在卫星本地天顶）与正视（以拍摄光锥中点为取景中心）之间切换。
- **锁定态自由缩放**：缩放改为叠加在本位取景距离上的倍率，换轨道 / 换视角不会被本位距离拉回。
- **内置卫星库扩充到 180 颗**并新增「通信中继」分组，列表支持滚动与搜索。
- **地球贴图升级**为 NASA 8192×4096 昼夜贴图，并叠加按相机距离衰减的地形凹凸光照与海面高光。

### 修复

- 锁定态左键拖拽时视角方向反复跳动：改为与未锁定态语义一致（左键绕当前视角中心旋转、右键移动视角中心、滚轮缩放），锁定期间 OrbitControls 完全停用，相机由 `src/core/followCamera.ts` 独占驱动。
- 缩小视图时星空被地球覆盖：星空球改为每帧跟随相机位置，按无限远背景处理。
- 极地轨道卫星锁定时无法转到「卫星在上、地球在下」的取景（极区角度退化），俯视 / 正视两套机位对极轨、静止与大椭圆轨道均可用。
- 高处卫星光点太小难以点中：新增点击轨道线直接锁定该卫星（屏幕空间点到线段距离判定，地球背面的线段剔除）。
- 城市高亮不点亮：`footprint.nadirUnitEci()` 原先返回星下点的对跖点，导致掠过判定落在卫星背面；现按星下点本身返回并补回归测试。
- 地球贴图与自转角、城市坐标的配准错位。

---

## v0.1 – v0.2 · 2026-09-16

**开发期** — Scaffolding and the first runnable build.

- **v0.2**：首个可运行版本。三类轨道（赤道低轨 / 极轨 / 太阳同步）起飞，随后补齐 9 类典型轨道 × 2 颗 = 18 颗预置卫星，全部真实在轨、真实 TLE，位置由 SGP4 + 本机系统时间实时推算。点击卫星光点放大并锁定跟随，锁定时用高亮光锥与地面覆盖圈显示拍摄范围，光锥扫过的城市名变色高亮。卫星详情卡（轨道类型 ⓘ 悬停说明、高度、速度、周期、倾角、偏心率、星下点经纬度、视场角、中英简介）。添加自己的卫星：粘贴 TLE、按轨道根数生成（6 个模板）、从内置卫星库挑选；自定义卫星存 localStorage 并可导出 / 导入 JSON。中英文切换、时间倍率（×1 / ×10 / ×60 / ×600）与暂停、图层开关、画质降级。
- **v0.1**：项目脚手架与技术方案定稿——Vite + TypeScript + three.js + satellite.js，ESLint / Prettier / Vitest，MIT 许可，GitHub Pages 自动部署工作流。
